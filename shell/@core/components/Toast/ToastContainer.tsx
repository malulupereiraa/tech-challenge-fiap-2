'use client';

import styled from 'styled-components';

export default styled.div`
  .toast-container {
    margin-top: 72px;
  }

  .toast {
    background-color:#FFFFFF;
    border-style: none;
    box-shadow: none;
    padding: 0px;
  }

  .toast-body {
    padding: 15px;
  }

  .toast-content {
    display:flex;
    align-items: center;
    font-size: 16px;
  }

  h6 {
    font-weight: 700;
  }

  svg {
    margin-right: 9px;
    width: 28px;
    height: 28px;
  }
`;
